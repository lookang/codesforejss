// goto Initialization and paste this if the MathJax is required at first
try { // allow code to run in Student Learning Space 
  MathJax.Hub.Queue(["Typeset",MathJax.Hub]);
} catch(e) {
  MathJax = false
}

//goto Fixed relations and paste this if the MathJax is required all the time
MathJax.Hub.Queue(["Typeset",MathJax.Hub]);

//goto Information about EJSS, HTML head and paste this
<script type="text/javascript" async="true"
 src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.1/MathJax.js?config=TeX-MML-AM_CHTML">
</script>

//in your formula 
// syntax is "$$ \\frac{}{} $$" for fraction
formula = "$$ \\frac{"+k.substring(0,k.indexOf("/"))+"}{"+k.substring(k.indexOf("/")+1)+"}    $$";

//display in a panel - Html
//call the Html formula
 
